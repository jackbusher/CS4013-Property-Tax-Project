import java.io.*;
import java.util.*;

public class DepartmentEnvRecord {
    private ArrayList <Property> PropertyOutput;
    public DepartmentEnvRecord() {

    }
    
    public String getTaxForProperty (String Address) {
        PropertyOutput = newArrayList <Property>();
        PropertyOutput = 
        
        for(int  i =  0; i < PropertyOutput.size();  i++) {
            if(address.equals((PropertyOutput.get(i)).getAddress())) {
                return (PropertyOutput.get(i)).toString();
            }
        }
            return "Try a different address";
    }
    
    public String getOverdueTax (int year){
    
    }
    
    public String getOverdueTax (int year, String eircode) {
    
    } 
    
    public String getTaxForOwner (String name) {
    
    }
    
    
}
